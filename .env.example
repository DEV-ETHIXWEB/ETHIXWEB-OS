# Base URL of the TeamFlow Express + MongoDB backend.
# Local dev: http://localhost:4000
# Railway single-service deploy: leave this unset so the app uses the same domain.
VITE_API_URL=http://localhost:4000
